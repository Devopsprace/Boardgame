# Unique project key in SonarQube
sonar.projectKey=BoardGame

# Human-readable project name
sonar.projectName=BoardGame

# Project version
sonar.projectVersion=1.0

# Source code folder
sonar.sources=src

# Folder with compiled classes
sonar.java.binaries=target

# Optional: language (Sonar can auto-detect)
sonar.language=java

# Encoding of the source files
sonar.sourceEncoding=UTF-8
# Unique project key in SonarQube
sonar.projectKey=BoardGame

# Human-readable project name
sonar.projectName=BoardGame

# Project version
sonar.projectVersion=1.0

# Source code folder
sonar.sources=src

# Folder with compiled classes
sonar.java.binaries=target

# Optional: language (Sonar can auto-detect)
sonar.language=java

# Encoding of the source files
sonar.sourceEncoding=UTF-8

